This is just a small bash script to decode webp files to png

You must provide de origin folder, the target folder, and the base name of the final files

eg.:

./webp2png.sh -o /path/to/webp/files -f /path/to/final/folder -n <base file name>

